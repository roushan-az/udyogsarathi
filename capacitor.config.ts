import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.udyogsarathi.app',
  appName: 'udyog-sarathi-frontend',
  webDir: 'dist'
};

export default config;
